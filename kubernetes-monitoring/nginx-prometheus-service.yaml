apiVersion: v1
kind: Service
metadata:
  name: custom-nginx-metrics
  namespace: monitoring
  labels:
    app: custom-nginx
spec:
  selector:
    app: custom-nginx
  ports:
  - name: http
    port: 80
    targetPort: 80
  - name: metrics
    port: 9113
    targetPort: 9113
  type: ClusterIP
