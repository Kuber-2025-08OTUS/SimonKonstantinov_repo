# Что отправлять на удаленный сервер (репозиторий)

## 📋 Минимальный набор (обязательно)

По требованиям задания необходимо отправить:

### 1. Выводы kubectl get nodes

```
outputs/
├── nodes-before-upgrade.txt  ✅ (уже есть)
└── nodes-after-upgrade.txt   ⏳ (создать после обновления)
```

### 2. Все команды для воспроизведения

**Главный файл со всеми командами:**
```
commands/ALL_COMMANDS.md  ⭐
```

Этот файл содержит:
- ✅ Все команды для подготовки master-ноды
- ✅ Все команды для подготовки worker-нод  
- ✅ Все команды для инициализации кластера
- ✅ Все команды для обновления кластера

## 📦 Полный набор (рекомендуется)

Для удобства проверки отправьте:

```
kubernetes-prod/
├── README.md                    # Описание проекта
├── commands/
│   ├── ALL_COMMANDS.md          # ⭐ ВСЕ команды в одном файле
│   ├── 01-prepare-master.md     # (опционально) Детали по master
│   ├── 02-prepare-worker.md     # (опционально) Детали по worker
│   ├── 03-init-cluster.md       # (опционально) Детали инициализации
│   ├── 04-upgrade-cluster.md    # (опционально) Детали обновления
│   └── README.md                # Описание структуры
└── outputs/
    ├── nodes-before-upgrade.txt  # ✅ Вывод до обновления
    └── nodes-after-upgrade.txt   # ⏳ Вывод после обновления
```

## ⚠️ Что НЕ отправлять

- ❌ `k8s-key` (приватный SSH ключ)
- ❌ `vms-info.txt` (может содержать пароли)
- ❌ Временные файлы

**Примечание:** `.gitignore` уже настроен для исключения приватных ключей.

## ✅ Чеклист перед отправкой

- [x] `outputs/nodes-before-upgrade.txt` - есть ✅
- [ ] `outputs/nodes-after-upgrade.txt` - создать после обновления
- [x] `commands/ALL_COMMANDS.md` - все команды задокументированы ✅
- [x] Приватные ключи исключены через `.gitignore` ✅

## 🎯 Итог

**Минимум для задания:**
1. `commands/ALL_COMMANDS.md` - все команды
2. `outputs/nodes-before-upgrade.txt` - вывод до обновления
3. `outputs/nodes-after-upgrade.txt` - вывод после обновления

**Этого достаточно для проверки и воспроизведения всех действий!**